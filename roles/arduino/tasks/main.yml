---
- name: "ide | install"
  dnf: name=arduino state=latest
  become: yes

- name: "groups | add"
  user: name={{ ansible_user_id }} append=yes
        groups={{ item }}
  with_items:
    - dialout
    - lock
  become: true
