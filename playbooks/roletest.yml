---
#- include: "vbox.yml"

- name: RoleTest
  hosts: all
  vars:
    - secure_firewalld: true
    - firewalld_zone: home
    - firewalld_enabled_services:
        - ssh
  roles:
    #- aws
    - wireshark

  tasks:
    
  vars_files:
    - ../vars/global.yml
    #- ../vars/ctct.yml
    #- ../vars/home.yml
    #- ../vars/emc.yml
