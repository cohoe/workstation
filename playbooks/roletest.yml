---
- name: RoleTest
  hosts: all
  vars:
    - firewalld_zone: work
  roles:
    #- xfce
    #- docky
    #- launchy
    #- chrome/browser
    #- common
    #- xfce
    #- plex
    #- security
    #- grub
    #- samba
    #- httpd
    #- nfs
    - pipeline
  vars_files:
    - ../vars/global.yml
    #- ../vars/emc.yml
