---
- name: RoleTest
  hosts: localhost
  vars:
    - firewalld_zone: home
    - firewalld_enabled_services:
        - ssh
  tasks:
    - import_role:
        name: android
#        tasks_from: login
    
  vars_files:
    - ../vars/global.yml
#    - ../vars/ctct.yml
#    - ../vars/home.yml
