---
- name: RoleTest
  hosts: localhost
  vars:
    - firewalld_zone: work
    - firewalld_enabled_services:
        - ssh
  roles:
    - sudo
    - clitools
    - shell 
#    - networkmanager
#    - firewalld
#    - ssh
#    - remmina
#    - httpd
#    - ctct
#    - aws
#    - apacheds
#    - openshift
#    - launchy
#    - ferdi
#    - apacheds
#  tasks:
#    - import_role:
#        name: networkmanager
#        tasks_from: dns
#      become: yes
    
  vars_files:
    - ../vars/global.yml
    - ../vars/eagle.yml
    #- ../vars/home.yml
