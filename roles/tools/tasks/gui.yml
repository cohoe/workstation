---
- name: "GUI tools"
  dnf: name={{ item }} state=latest
  with_items:
    - numlockx
  sudo: true

- name: "vlc | install"
  dnf: name=vlc state=latest
  sudo: yes
  when: '"rpmfusion" in use_repos'
