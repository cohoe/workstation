---
- name: install packages
  dnf:
    name:
      - python3-boto3
      - awscli
    state: latest
  become: yes

- name: create config directory
  file:
    path: '~/.aws'
    state: directory

- name: create default config
  copy:
    src: config
    dest: '~/.aws/config'
    mode: 0600
