---
# A home computer
#
#- include: base.yml
#- include: desktop.yml

- name: Home
  hosts: all
  vars:
    - firewalld_enabled_ports:
        - 32400/tcp # Plex
    - firewalld_zone: home
    - firewalld_enabled_services:
        - ssh
        - samba-client
        - samba
        - nfs
  roles:
#    - security
#    - hdhomerun
#    - arduino
#    - chrome/remote
#    - plex
    - samba
    - nfs
  vars_files:
    - ../vars/global.yml
