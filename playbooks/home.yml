---
# A home computer
#
- include: base.yml
- include: desktop.yml

- name: Home
  hosts: all
  roles:
    - hdhomerun
    - arduino
    - chrome/remote
  vars:
    - firewalld_zone: home
  vars_files:
    - ../vars/global.yml
