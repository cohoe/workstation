---
- name: RoleTest
  hosts: localhost
  vars:
    - secure_firewalld: true
    - firewalld_zone: home
    - firewalld_enabled_services:
        - ssh
  roles:
    - ssl
    
  vars_files:
    - ../vars/global.yml
#    - ../vars/ctct.yml
    - ../vars/home.yml
