---
#- include: "vbox.yml"

- name: RoleTest
  hosts: all
  vars:
  roles:
    - grub
    - shell

  tasks:
    
  vars_files:
    - ../vars/global.yml
    #- ../vars/ctct.yml
    #- ../vars/home.yml
    #- ../vars/emc.yml
