---
- name: RoleTest
  hosts: localhost
  vars:
    - secure_firewalld: true
    - firewalld_zone: home
    - firewalld_enabled_services:
        - ssh
  roles:
    #- ssl
    #- cinnamon
    #- shell
    #- backups/nas
    - nas

  tasks:
    #- include_tasks: ../roles/cinnamon/tasks/junk.yml
    
  vars_files:
    - ../vars/global.yml
#    - ../vars/ctct.yml
    - ../vars/home.yml
