---
- name: RoleTest
  hosts: localhost
  vars:
    - firewalld_zone: home
    - firewalld_enabled_services:
        - ssh
  roles:
    #- franz
  tasks:
    - import_role:
        name: security
        tasks_from: keys
    
  vars_files:
    - ../vars/global.yml
    - ../vars/ctct.yml
#    - ../vars/home.yml
