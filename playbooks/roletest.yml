---
- name: RoleTest
  hosts: localhost
  vars:
    - firewalld_zone: home
    - firewalld_enabled_services:
        - ssh
  roles:
    - dns
    - launchy
    
  vars_files:
    - ../vars/global.yml
    - ../vars/ctct.yml
#    - ../vars/home.yml
