---
- name: RoleTest
  hosts: localhost
  vars:
    - firewalld_zone: home
    - firewalld_enabled_services:
        - ssh
  roles:
    #- ssl
    #- cinnamon
    #- shell
    #- backups/nas
    #- nas
    #- vlc
    #- apacheds
    #- franz
    - pycharm

  tasks:
    #- include_tasks: ../roles/cinnamon/tasks/junk.yml
    #- include_tasks: ../roles/cinnamon/tasks/login.yml
    
  vars_files:
    - ../vars/global.yml
#    - ../vars/ctct.yml
#    - ../vars/home.yml
