---
- include: keys.yml

- include: firewall.yml
  when: secure_firewalld
  become: yes

- include: sshd.yml
  become: true
  when: secure_sshd

- name: generate ssh key
  shell: ssh-keygen -t rsa -f ~/.ssh/id_rsa -N ''
