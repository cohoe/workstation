---
- name: RoleTest
  hosts: localhost
  vars:
    - firewalld_zone: work
    - firewalld_enabled_services:
        - ssh
  roles:
    - vscode
    - swift
#  tasks:
#    - import_role:
#        name: networkmanager
#        tasks_from: dns
#      become: yes
    
  vars_files:
    - ../vars/global.yml
#- ../vars/eagle.yml
#    - ../vars/home.yml
