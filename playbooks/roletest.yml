---
- name: RoleTest
  hosts: localhost
  vars:
    - firewalld_zone: home
    - firewalld_enabled_services:
        - ssh
  tasks:
    - import_role:
        name: security
        tasks_from: firewall
      become: yes
    
  vars_files:
    - ../vars/global.yml
#    - ../vars/ctct.yml
    #- ../vars/home.yml
