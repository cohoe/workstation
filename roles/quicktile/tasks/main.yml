---
- name: "packages | install"
  dnf: name={{ item }} state=latest
  sudo: yes
  with_items:
    - pygtk2
    - pygobject2
    - dbus-python
    - gnome-python2-libwnck
    - python-xlib

- name: "target | create"
  file: path=/opt/quicktile/
        state=directory
  sudo: yes

- name: "repo | install"
  git: repo=https://github.com/ssokolow/quicktile
       dest=/opt/quicktile
  sudo: yes

- name: "shortcut | install"
  copy: src=quicktile.desktop
        dest={{ sys_shortcuts_dir }}/quicktile.desktop
  sudo: yes

- name: "autostart | install"
  file: src={{ sys_shortcuts_dir }}/quicktile.desktop
        dest={{ user_autostart_dir }}/quicktile.desktop
        state=link
