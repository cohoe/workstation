---
- include: chrome.yml
  sudo: true
  when: '"chrome" in use_repos'

- include: slack.yml
  sudo: true
  when: '"slack" in use_repos'

- include: rpmfusion.yml
  sudo: true
  when: '"rpmfusion" in use_repos'

- include: flash.yml
  sudo: true
  when: '"flash" in use_repos'
