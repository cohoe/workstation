[DEFAULT]
bantime = 4294967 # this is the max ipset allows
findtime = 10m
maxretry = 2
destemail = {{ mail_to_address }}
sender = {{ mail_from_address }}
mta = postfix
ignoreip = {{ fail2ban.exclude | join(' ') }}

[sshd]
enabled = true
