---
- name: RoleTest
  hosts: localhost
  vars:
    - firewalld_zone: work
    - firewalld_enabled_services:
        - ssh
  roles:
    #- zoom
    #- docker
    #- vbox/host
    - ferdium
#  tasks:
#    - import_role:
#        name: cinnamon
#        tasks_from: screenshot
#      become: yes
    
  vars_files:
    - ../vars/global.yml
#- ../vars/eagle.yml
    #- ../vars/home.yml
