---
# A home computer
#
- include: base.yml
- include: desktop.yml

- name: Home
  hosts: all
  vars:
    - firewalld_zone: home
  roles:
    - security
    - hdhomerun
    - arduino
    - chrome/remote
  vars_files:
    - ../vars/global.yml
