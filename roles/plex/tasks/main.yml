---
#- name: "install"
#  dnf: name={{ plex_package }} state=latest
#  sudo: yes

- name: "service | enable"
  action: service name=plexmediaserver
                  state=started
                  enabled=yes
  sudo: yes
