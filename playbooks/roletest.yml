---
- name: RoleTest
  hosts: localhost
  vars:
    - firewalld_zone: home
    - firewalld_enabled_services:
        - ssh
  tasks:
    - import_role:
        name: maxime
        #tasks_from: iscsi
    
  vars_files:
    - ../vars/global.yml
#    - ../vars/ctct.yml
    - ../vars/home.yml
