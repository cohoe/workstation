---
- name: keyboard shortcuts
  dconf:
    key: "{{ item.key }}"
    value: "{{ item.value }}"
  with_dict:
    /org/cinnamon/desktop/keybindings/looking-glass-keybinding: "@as []"
    /org/cinnamon/desktop/keybindings/media-keys/screensaver: "['<Super>l', 'XF86ScreenSaver']"
