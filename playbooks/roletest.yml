---
- name: RoleTest
  hosts: localhost
  vars:
    - firewalld_zone: home
    - firewalld_enabled_services:
        - ssh
  roles:
    #- cinnamon
  tasks:
    - import_role:
        name: cinnamon
        tasks_from: mouse
    
  vars_files:
    - ../vars/global.yml
#    - ../vars/ctct.yml
#    - ../vars/home.yml
