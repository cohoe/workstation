---
- name: install SSL CAs to trust
  get_url:
    url: "{{ item.url }}"
    dest: "/etc/pki/ca-trust/source/anchors/{{ item.dest }}"
    validate_certs: False
  with_items: "{{ sslca_list }}"
  become: true
  when: sslca_list is defined
  notify: update-ca-trust

- name: flush handlers
  meta: flush_handlers
